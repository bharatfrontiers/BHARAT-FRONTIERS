async function loadArticles(){const r=await fetch("./Data/articles.json");if(!r.ok)throw new Error("Unable to load article data");return r.json()}
function card(a){return `<article class="card"><a href="./Articles/article.html?slug=${encodeURIComponent(a.slug)}"><img src="./Articles/${a.image}" alt="${escapeHtml(a.title)}" loading="lazy"></a><div class="card-body"><div class="meta">${escapeHtml(a.section)} · ${escapeHtml(a.type)}</div><h3><a href="./Articles/article.html?slug=${encodeURIComponent(a.slug)}">${escapeHtml(a.title)}</a></h3><p>${escapeHtml(a.summary)}</p><a class="read" href="./Articles/article.html?slug=${encodeURIComponent(a.slug)}">Read article →</a></div></article>`}
function escapeHtml(s=""){return String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]))}
(async()=>{try{const data=await loadArticles();const pub=data.filter(a=>a.status==="published"||a.status==="draft");const f=document.getElementById("featured"),l=document.getElementById("latest");if(f)f.innerHTML=pub.filter(a=>a.featured).slice(0,6).map(card).join("");if(l)l.innerHTML=pub.slice().sort((a,b)=>b.date.localeCompare(a.date)).slice(0,6).map(card).join("")}catch(e){console.error(e)}})();
document.getElementById("searchBtn")?.addEventListener("click",()=>{const q=document.getElementById("homeSearch").value.trim();if(q)location.href="./Articles/?q="+encodeURIComponent(q)});
document.getElementById("homeSearch")?.addEventListener("keydown",e=>{if(e.key==="Enter")document.getElementById("searchBtn").click()});
document.getElementById("menuBtn")?.addEventListener("click",()=>document.getElementById("mainNav").classList.toggle("open"));
